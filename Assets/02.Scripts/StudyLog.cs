using UnityEngine;


// 유니티 에디터에있는 Consol View에 Log를 테스트하기 위한 클래스
public class StudyLog : MonoBehaviour
{
    // Start is called once | before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
    
    }

    // Update is called once per frame
    void Update()
    {
     
    }
}
